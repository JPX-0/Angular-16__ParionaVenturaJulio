import { RenderFormPipe } from './render-form.pipe';

xdescribe('RenderFormPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderFormPipe();
    expect(pipe).toBeTruthy();
  });
});
